{
  "name": "my-canvas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @canvas/app dev",
    "build": "yarn workspace @canvas/app build",
    "preview": "yarn workspace @canvas/app preview",
    "lint": "yarn workspace @canvas/app lint"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
